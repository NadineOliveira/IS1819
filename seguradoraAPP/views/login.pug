extends layout

block content
    .row
        .col-sm-6.offset-sm-3.text-center
            .card
                .card-header 
                    h1 Seguradora APP
                .card-body
                    .container
                        h5.card-title Login
                        br
                        .info-form
                            form.form-inlin.justify-content-center(method="POST" action="/")
                                .form-group
                                    label Email
                                        input.form-control(type="email" name="email")
                                .form-group
                                    label Password
                                        input.form-control(type="password" name="password")
                                button.btn.btn-success(type="submit") Submit





